using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Fix : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //dog jump skacz chuju
    }

    // Update is called once per frame
    void Update()
    {
      
    }
}
